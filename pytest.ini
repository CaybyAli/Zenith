[pytest]
addopts = -m "not ffmpeg_integration"
markers =
    smoke: smoke tests
    ffmpeg_integration: requires real ffmpeg/moviepy rendering and is skipped by default
testpaths = tests
norecursedirs =
    .git
    .pytest_cache
    pytest-cache-files-*